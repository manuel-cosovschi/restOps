/**
 * Placeholder. El Sprint 1 reemplaza esto por:
 *   - (auth)/login, (auth)/signup
 *   - (admin)/[orgSlug]/dashboard
 *   - (kiosk)/[orgSlug]/[locationSlug]
 * Ver CLAUDE.md > "Sprint 1 — que construir ahora".
 */
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="max-w-md text-center">
        <h1 className="text-2xl font-semibold">RestOps</h1>
        <p className="mt-2 text-sm text-neutral-500">
          Sprint 0 completo — base de datos lista. Sprint 1 arranca acá.
        </p>
      </div>
    </main>
  );
}
